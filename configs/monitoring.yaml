# Monitoring Runtime — MCP 서버 설정
#
# CloudWatch MCP 서버를 연결하여 메트릭/알람/로그를 분석합니다.

gateway:
  enabled: true

mcp_servers:
  - name: aws-cloudwatch
    enabled: true
    transport: stdio
    command: uvx
    args:
      - awslabs.cloudwatch-mcp-server@latest
    env:
      AWS_REGION: "${AWS_REGION}"
